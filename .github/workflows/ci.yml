# Stub CI — enable after Phase 1 adds package.json and test scripts.
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Phase 0 — docs only
        run: echo "CI will run lint + test after Phase 1 monorepo scaffold."

  # Uncomment after Phase 1:
  # build:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v4
  #       with:
  #         version: 9
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: pnpm
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm lint
  #     - run: pnpm test
  #     - run: pnpm build
